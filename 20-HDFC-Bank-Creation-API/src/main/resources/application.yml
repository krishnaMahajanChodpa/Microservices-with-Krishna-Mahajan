eureka:
  client:
    service-url:
      default-Zone: ${DISCOVERY_URL:http://localhost:8761}/eureka/
server:
  port: 1111
  
spring:
  application:
    name: BANK-CREATION-FROM
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    name: HDFC
    password: root
    url: jdbc:mysql://localhost:3306/HDFC
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect   
    
     
